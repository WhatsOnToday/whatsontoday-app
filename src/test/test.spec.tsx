import * as WhatsOnApi from "../main/backend/WhatsOnApi";

describe("API Test", () => {
    it("should be able to consume iterators", ()  => {
        expect(3).toBe(3);
        expect(3).toBe(3);
    });
});